{
  "name": "sc-api-getaway",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "make build",
    "start": "nodemon --watch . --exec 'make start' --signal SIGTERM --ext go,json,yaml,sql,env,sh",
    "serve": "nodemon --watch . --exec 'make start' --signal SIGTERM --ext go,json,yaml,sql,env,sh",
    "test:integration": "make test"
  },
  "keywords": [],
  "author": "karadyauran",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.7"
  }
}
